// ---------------------------------------------------------------------------
// src/cli/main.ts — `omd` CLI entry point (placeholder).
//
// Full command dispatch (install, uninstall, status, doctor, update) will be
// added in a follow-up slice. This placeholder exists so the build script
// (`scripts/build-cli.mjs`) can produce a working `dist/cli.mjs` artifact
// during Slice 1 (Foundation).
// ---------------------------------------------------------------------------

const main = (): void => {
  console.log("omd: use `omd install` to register opencode-rules-md");
};

main();
